# 安全策略

## 支持范围

本项目是一个持续部署的应用（运行在 Cloudflare Workers 上），不发布带版本号的正式发行版。安全修复只针对 `main` 分支的最新代码及其线上部署。

| 版本 | 是否提供安全更新 |
| ---- | ---------------- |
| `main`（最新） | :white_check_mark: |
| 其他历史提交 / fork | :x: |

## 报告漏洞

**请不要通过公开 issue、Pull Request 或讨论区提交安全漏洞。**

请使用 GitHub 的私密漏洞报告通道：

1. 打开仓库的 **Security** 标签页 → **Report a vulnerability**
   （https://github.com/aaoqqqdd/rent/security/advisories/new）
2. 如无法使用该通道，可通过仓库所有者的 GitHub 主页私信联系。

报告时请尽量包含：

- 受影响的功能 / 路由 / 文件
- 复现步骤或 PoC
- 影响评估（数据泄露、权限绕过、账户接管等）
- 已知的缓解方式（如有）

## 处理流程与时间预期

- **3 个工作日内**：确认已收到报告。
- **10 个工作日内**：完成初步评估，给出是否受理的结论及严重程度分级。
- **受理后**：在私密 advisory 中同步修复进展；修复合并并部署后再公开披露，并在致谢中注明报告者（除非报告者要求匿名）。
- **不受理时**：说明原因（例如不在威胁模型范围内、无法复现、属于第三方依赖问题）。

## 范围说明

以下内容通常**不视为**本项目漏洞：

- 依赖库自身的已知漏洞（请直接向上游报告；如影响本项目可一并告知）
- 需要物理访问设备或已被攻陷的账户 / 环境才能触发的问题
- 缺少某些安全加固响应头等最佳实践类建议（欢迎以普通 issue 提出）
- 社会工程、对 Cloudflare 等基础设施的攻击

## 敏感数据

本系统会处理客户信息、合同、付款凭证等敏感数据。请勿在报告或 PoC 中包含真实用户数据，测试时也请使用自建的测试账户与数据。
